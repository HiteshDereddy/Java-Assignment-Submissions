/*
2. Differences between HashMap, LinkedHashMap, and TreeMap:
   - HashMap: Unordered, allows null keys, fast operations (O(1) for put/get).
   - LinkedHashMap: Maintains insertion order.
   - TreeMap: Sorted by keys (natural ordering or custom Comparator), slower (O(log n) operations).
*/
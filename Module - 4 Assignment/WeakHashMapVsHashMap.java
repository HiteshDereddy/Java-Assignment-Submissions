/*
5. WeakHashMap vs HashMap:
   - WeakHashMap uses weak references for keys, allowing garbage collection when no strong reference exists.
   - HashMap holds strong references, preventing garbage collection.
*/
/*
5. Difference between Hashtable and HashMap:
   - Hashtable is synchronized (thread-safe) but slower.
   - HashMap is not synchronized (use ConcurrentHashMap for thread safety).
   - Hashtable does not allow null keys, but HashMap does.
   - Hashtable is considered legacy due to inefficiency.
*/